# Copy to .env and fill in real values. .env is gitignored.
# Keys listed under [mcp.env_inject] in deploy.toml are injected into local CLI configs on `python scripts/sync.py mcp`.

CONTEXT7_API_KEY=ctx7sk-your-key-here
